<p id="notice"><%= notice %></p>

<p>
  <strong>Shop:</strong>
  <%= @theme_install.shop_id %>
</p>

<p>
  <strong>Shop domain:</strong>
  <%= @theme_install.shop_domain %>
</p>

<p>
  <strong>Theme:</strong>
  <%= @theme_install.theme_id %>
</p>

<%= link_to 'Edit', edit_theme_install_path(@theme_install) %> |
<%= link_to 'Back', theme_install_url %>
